--- a/src/MistralChatMessageIterator.php	2026-03-19 15:43:10.723845522 +0000
+++ b/src/MistralChatMessageIterator.php	2026-03-19 15:53:46.737821860 +0000
@@ -15,10 +15,6 @@
  * PromptJsonDecoder may interrupt the first pass and need to continue
  * consuming remaining chunks from where they left off.
  *
- * The "advance before yield" pattern ensures that when a consumer breaks out
- * of the iteration, the underlying iterator is already positioned at the
- * next unread chunk, preventing duplication on the second pass.
- *
  * @see https://www.drupal.org/project/ai_provider_mistral/issues/3572398
  */
 class MistralChatMessageIterator extends StreamedChatMessageIterator {
@@ -29,29 +25,42 @@
    * {@inheritdoc}
    */
   public function doIterate(): \Generator {
-    // Use manual iteration to avoid foreach's implicit rewind() call.
-    // PHP Generators auto-start on the first valid()/current() call, and
-    // maintain their position across multiple doIterate() invocations since
-    // $this->iterator is shared state on the object.
+    // Use manual iteration to avoid foreach's implicit rewind() call on
+    // the underlying Generator. PHP Generators auto-start on the first
+    // valid()/current() call and maintain their position across multiple
+    // doIterate() invocations since $this->iterator is shared state.
     while ($this->iterator->valid()) {
       $response = $this->iterator->current();
-      // Advance BEFORE yielding so that when the consumer breaks after
-      // receiving a chunk, the underlying iterator is already past it.
-      $this->iterator->next();
 
       if (!$response instanceof Response) {
+        $this->iterator->next();
         continue;
       }
+
+      // Capture all data from the current chunk BEFORE advancing the
+      // iterator. The SSE client reuses a single Response object, so
+      // calling next() overwrites getChunk() and other accessors with
+      // the next event's data.
+      $chunkText = $response->getChunk() ?? '';
       $message = $response->getLastMessage();
+      $role = $message->getRole() ?? 'assistant';
       $metadata = $response->getUsage() ?: [];
-
       $toolCalls = $response->getToolCalls();
+      $rawResponse = $this->responseToArray($response);
+      $stopReason = $response->getStopReason();
+
+      // Advance AFTER capturing data so that the consumer sees the
+      // correct chunk text. When the consumer breaks out of the loop,
+      // the next doIterate() call will resume from the right position
+      // since $this->iterator is shared state.
+      $this->iterator->next();
+
       $streamedMessage = $this->createStreamedChatMessage(
-        $message->getRole() ?? 'assistant',
-        $response->getChunk() ?? '',
+        $role,
+        $chunkText,
         $metadata,
-        $toolCalls && !$toolCalls->isEmpty() ? $toolCalls->toArray() : NULL,
-        $this->responseToArray($response),
+        $toolCalls && !$toolCalls->isEmpty() ? $this->serializeToolCalls($toolCalls) : NULL,
+        $rawResponse,
       );
 
       // Set token usage if available.
@@ -71,7 +80,6 @@
       }
 
       // Set finish reason if available.
-      $stopReason = $response->getStopReason();
       if ($stopReason) {
         $this->setFinishReason($stopReason);
       }
@@ -80,4 +88,36 @@
     }
   }
 
+  /**
+   * Converts ToolCallCollection items into the format expected by
+   * StreamedChatMessageIterator::assembleToolCalls().
+   *
+   * The base class calls $tool->toArray() on each element and expects
+   * ['function']['arguments'] to be a JSON string for concatenation.
+   * ToolCallFunction::toArray() returns arguments as a decoded array,
+   * so we wrap each item in a simple object with a toArray() that
+   * JSON-encodes the arguments.
+   *
+   * @param \Partitech\PhpMistral\Tools\ToolCallCollection $toolCalls
+   *   The tool call collection from the Mistral response.
+   *
+   * @return array
+   *   Array of objects compatible with assembleToolCalls().
+   */
+  private function serializeToolCalls($toolCalls): array {
+    $result = [];
+    foreach ($toolCalls as $tc) {
+      $data = $tc->toArray();
+      // Ensure arguments is a JSON string for the base class.
+      if (isset($data['function']['arguments']) && is_array($data['function']['arguments'])) {
+        $data['function']['arguments'] = json_encode($data['function']['arguments']);
+      }
+      $result[] = new class($data) {
+        public function __construct(private array $data) {}
+        public function toArray(): array { return $this->data; }
+      };
+    }
+    return $result;
+  }
+
 }
--- a/src/Plugin/AiProvider/MistralProvider.php	2026-03-19 15:43:10.725845487 +0000
+++ b/src/Plugin/AiProvider/MistralProvider.php	2026-03-19 15:44:14.626727787 +0000
@@ -252,6 +252,14 @@
       else {
         $this->client = new MistralClient($this->apiKey);
       }
+      // Inject a Guzzle client configured for true HTTP streaming.
+      // The default PSR-18 adapter buffers the entire response body
+      // before returning, which defeats SSE streaming. Setting
+      // 'stream' => true makes Guzzle return a lazy-reading body
+      // so chunks arrive as the LLM generates them.
+      $guzzle = new \GuzzleHttp\Client(['stream' => true]);
+      $adapter = new \Http\Adapter\Guzzle7\Client($guzzle);
+      $this->client->setClient($adapter);
     }
   }
 

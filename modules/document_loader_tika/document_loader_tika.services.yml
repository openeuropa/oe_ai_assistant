services:
  _defaults:
    autowire: true

  # HTTP client for the Tika server, shared by the loader plugin and the
  # status report.
  Drupal\document_loader_tika\TikaClientInterface:
    class: Drupal\document_loader_tika\TikaClient

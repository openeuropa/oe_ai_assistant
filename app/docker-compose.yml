services:
  app:
    image: mcr.microsoft.com/playwright:v1.53.2-noble
    working_dir: /app
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - CI=true
    command: sleep infinity
    init: true

volumes:
  node_modules:

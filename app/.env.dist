# Standalone frontend development defaults to fixture-backed mock drafting.
# You do not need to copy this file for `npm run dev`.

# Optional: set the provider model used by `npm run dev:integration`.
GPT_AT_EC_MODEL=gpt-5.1

# Required only for provider-backed drafting integration mode.
KEY_AI_GPT_AT_EC=

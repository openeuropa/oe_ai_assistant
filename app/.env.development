# Dev-only plugins (echo, notes) for stress-testing the architecture.
# The Vite dev server reads this file automatically. For a Drupal-importable
# production build with the same plugins, run `DEV_PLUGINS=true npm run build`.
DEV_PLUGINS=true
